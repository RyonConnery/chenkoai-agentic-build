{
  "name": "chenkoai-agentic-build",
  "version": "0.1.0",
  "private": true,
  "description": "Independent ChenkoAI agentic AI software platform.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @chenkoai/api",
    "check": "npm run check --workspaces --if-present",
    "check:all": "powershell -ExecutionPolicy Bypass -File scripts/check-all.ps1",
    "db:migrate": "powershell -ExecutionPolicy Bypass -File scripts/apply-migrations.ps1",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "prettier": "^3.6.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=22"
  }
}
